import { DisableArrowsDirective } from './disable-arrows.directive';

describe('DisableArrowsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableArrowsDirective();
    expect(directive).toBeTruthy();
  });
});
